name: CodeQL

on:
  push:
    branches: [source]
  pull_request:
    branches: [source]
  schedule:
    - cron: '32 20 * * 0'

jobs:
  analyze:
    uses: homebridge/.github/.github/workflows/codeql-analysis.yml@latest
